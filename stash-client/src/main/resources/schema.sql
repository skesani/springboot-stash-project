CREATE TABLE IF NOT EXISTS data_item1 (
                                          id BIGINT AUTO_INCREMENT PRIMARY KEY,
                                          ssn VARCHAR(255) NOT NULL,
                                          address VARCHAR(255),
                                          phone_number VARCHAR(255),
                                          email VARCHAR(255)
);
